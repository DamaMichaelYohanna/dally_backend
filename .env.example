# Django Settings
SECRET_KEY=django-insecure-)0!@t40q)qtk#b)u5-j&5vc=b&!*4-+$crytu7r_og=b-3_i+h
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (SQLite is default, uncomment for PostgreSQL/MySQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/dally_db
# DATABASE_URL=mysql://user:password@localhost:3306/dally_db

# Email Configuration (Gmail)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Email Backend (use console for development, smtp for production)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# JWT Token Settings (in minutes/days)
ACCESS_TOKEN_LIFETIME_HOURS=1
REFRESH_TOKEN_LIFETIME_DAYS=7

# API Documentation
API_TITLE=Dally Bookkeeping API
API_DESCRIPTION=A comprehensive bookkeeping API with multi-user support, JWT authentication, and transaction management
API_VERSION=1.0.0
